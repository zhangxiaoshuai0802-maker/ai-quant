# reflection.md - 复盘与改进记录

## 2026-06-18 - 初版完成

### 完成内容

1. ✅ 创建项目目录结构（`data/raw`、`data/processed`、`notebooks`、`dashboard`、`outputs`）
2. ✅ 编写数据获取脚本（`fetch_data.py`），支持缓存机制和 API 失败回退
3. ✅ 创建研究 Notebook（`task01_research.ipynb`），完成基础部分
4. ✅ 生成基础图表（收盘价走势、成交量）

### 已知限制

| 限制 | 说明 | 改进方向 |
|------|------|----------|
| 前复权数据获取失败 | Tushare 免费账号权限不足，`ts.pro_bar(adj='qfq')` 返回空 | 升级 Tushare 积分，或手动计算复权因子 |
| 缓存元数据简单 | 当前仅保存 JSON，未实现缓存过期检查 | 添加缓存年龄检查，超过 N 天提示刷新 |
| Notebook 未完全执行 | 仅完成基础部分，数据质量检查留到下一轮 | 按用户要求分阶段完成 |

### 安全记录

- ✅ Token 从环境变量读取，未硬编码
- ✅ `.env` 文件不提交（已加入 `.gitignore`）
- ✅ 未打印、未保存 Token 值到任何提交文件

### 数据记录

- **数据范围**：2021-06-21 至 2026-06-18（共 1211 个交易日）
- **数据来源**：Tushare Pro API（`pro.fund_daily()` 接口）
- **缓存状态**：实时 API 成功，已更新本地缓存
- **前复权**：获取失败（权限不足），当前使用未复权数据

### 下一步

1. 完成数据质量检查（缺失值、异常值、交易日连续性）
2. 添加更多技术指标（RSI、MACD、布林带）
3. 生成 HTML 看板（支持 `file://` 协议离线打开）
4. 如权限允许，获取前复权数据并对比分析

---

**最后更新**：2026-06-18  
**负责人**：waanng
